const { CV_MAP, PERFIL_MAP, BUSQUEDA_MAP, PRACTICA_MAP } = require('./inputMap');

const inputFinder = (input) => {
    input = input.toLowerCase.split(' ');
    if (input.contains()) AAAAAAAAAAAAAAAAAAAAAAAA
    if (type) query = { ...query, include: [{ model: Type, where: { name: titleCase(type) } }] };
}

module.exports = inputFinder;